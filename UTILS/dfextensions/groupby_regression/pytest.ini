[pytest]
testpaths = tests
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    feature(id): capability feature ID from feature_taxonomy.py
    layer(name): test quality layer — invariance|integration|smoke|validation|performance
    roofline: Roofline performance regression tests
addopts = --strict-markers
